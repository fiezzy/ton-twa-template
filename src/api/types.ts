export type PostFileType = {
  filename: string
  content: string
}

export type ProfileInfoType = {
  email: string
  name: string
  telegram: string
  walletAddress?: string
  referrer_id: string
  image?: string
  telegramInitData: string
}
